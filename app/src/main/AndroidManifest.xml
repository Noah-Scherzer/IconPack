<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Application-Tag -->
    <application
        android:name=".IconPackApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name">

        <!-- Beispiel: Activity für ein leeres UI oder About-Screen -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Meta-Daten, die von einigen Launchern ausgelesen werden -->
        <meta-data
            android:name="com.android.launcher.theme"
            android:value="com.example.myiconpack" />

        <!-- Intent-Filter für Nova-Launcher und andere -->
        <intent-filter>
            <action android:name="com.novalauncher.THEME"/>
            <category android:name="android.intent.category.DEFAULT"/>
        </intent-filter>

    </application>

</manifest>
